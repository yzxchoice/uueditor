declare class StyleHSlider extends StyleBase {
    private sd_hSlider;
    constructor(config: any, props: any);
    private onAddToStage();
    private initEvent();
    private changeHandler(evt);
}
